#include <algorithm>
#include <cmath>
#include <cstring>
#include <iostream>
#include <map>
#include <numeric>  //iota
#include <queue>
#include <set>
#include <string>
#include <unordered_map>
#include <vector>
#define ll long long
using namespace std;

const int N =

    struct Node {
  ll l, r;
  ll Gcd;
} tr[N * 4];

signed main() {
  cin.tie(nullptr)->sync_with_stdio(false);

  return (0 ^ 0);
}
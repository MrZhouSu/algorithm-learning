#include <iostream>
#include <algorithm>
#include <cstring>

using namespace std;

typedef pair<int, int> PII;

const int N = 110;

int n, m;
int g[N][N];
int d[N][N];
PII q[N];

int bfs()
{
    int hh = 0, tt = 0;
    q[0] = {0, 0};
}

int main(void)
{
    system("pause");
    return 0;
}